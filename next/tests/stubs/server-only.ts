// Test stub for the `server-only` guard. In a real build the guard is enforced by the
// bundler (it throws if a server module is pulled into a client bundle); under vitest we
// alias it to this no-op so server modules can be unit-tested in the node environment.
export {};
