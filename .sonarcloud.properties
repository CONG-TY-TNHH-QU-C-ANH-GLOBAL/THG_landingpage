# SonarCloud automatic-analysis configuration.
#
# TEMPORARY migration-window CPD scope (WEB-001 / migration program, 2026-07-17):
# the legacy Vite app (src/**, root tailwind.config.ts) and the ported Next app
# (next/**) intentionally coexist in this repository until the M10 cutover; every
# cross-stack duplicate pair is a verbatim parity port, and governance forbids
# touching the legacy side until M11 (ADR-001 Option A).
#
# Scope: DUPLICATION DETECTION ONLY, LEGACY SIDE ONLY.
#  - next/** stays fully duplication-scanned (Next-internal duplication is still caught).
#  - The legacy tree stays in scope for every other analysis (issues, hotspots, metrics).
#
# REMOVAL TRIGGER (recorded on queue item Q-024): the M11 PR that deletes the legacy
# Vite source, or production cutover — whichever comes first. Removal verification:
# delete this exclusion, run a full analysis on the Next-only tree, require New Code
# duplication <= 3% with no hidden Next-internal duplication.
sonar.cpd.exclusions=src/**,tailwind.config.ts
